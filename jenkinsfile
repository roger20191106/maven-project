pipeline {
    agent any
    tools{
        maven 'local maven'
    }
    stages{
        stage ('Build'){
            steps{
                echo 'Stat building ...'
                sh 'mvn clean package'
            }
			post{
				success{
					echo 'Star to archive...'
					archiveArtifacts artifacts: '**/target/*.war'
				}
			}
        }
    } 
}