pipeline {
    agent any
    tools{
        maven 'local maven'
    }
    stages{
        stage ('Build'){
            steps{
                echo 'Start building ...'
                call mvn clean
                echo 'Builded!'
            }
			post{
				success{
					echo 'Star to archive...'
					archiveArtifacts artifacts: '**/target/*.war'
				}
			}
        }
    } 
}